namespace: backing

bases:
- ../namespace

transformers:
- pgo-configmap-fixer.yaml

resources:
# https://raw.githubusercontent.com/CrunchyData/postgres-operator/v4.4.0/installers/kubectl/postgres-operator.yml
- postgres-operator.yaml
